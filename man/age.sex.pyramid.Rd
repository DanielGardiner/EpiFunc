% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age.sex.pyramid.R
\name{age.sex.pyramid}
\alias{age.sex.pyramid}
\title{Create age-sex pyramid}
\usage{
age.sex.pyramid(data, age.grp.col, sex.col, lower.limit = NULL,
  upper.limit = NULL, split.by = NULL, col.pal = 1)
}
\arguments{
\item{data}{a data.frame}

\item{age.grp.col}{a character defining the age-group column within the data.frame}

\item{sex.col}{a character defining the sex column within the data.frame}

\item{lower.limit}{a numeric defining the lower limit for the x-axis}

\item{upper.limit}{a numeric defining the upper limit for the x-axis}

\item{split.by}{a character defining a column to facet by within the data.frame}

\item{col.pal}{a numeric defining the colour palette to use}
}
\value{
an age-sex pyramid
}
\description{
Create age-sex pyramid
}
\examples{
# set dummy data
set.seed(5)

data = data.frame(sex = sample(c("Male", "Female", "Unknown"), 200, replace = T),
                  age = sample(c(NA, 1:100), 200, replace = T),
                  status = sample(c("Confirmed", "Probable"), 200, replace = T))

data$age.grp = cut(as.numeric(data$age), breaks = c(0, 6, 16, 26, 46, 66, Inf),
                   include.lowest = T)

# age sex pyramid

age.sex.pyramid(data, age.grp.col = "age.grp", sex.col = "sex", split.by = NULL)

# age sex pyramid with lower/upper limit and colour palette

age.sex.pyramid(data, age.grp.col = "age.grp", sex.col = "sex", split.by = NULL,
                lower.limit = -50, upper.limit = 40, col.pal = 4)

# age sex pyramid with facet

age.sex.pyramid(data, age.grp.col = "age.grp", sex.col = "sex", split.by = "status")



}
\author{
Daniel Gardiner (daniel.gardiner@phe.gov.uk)
}
